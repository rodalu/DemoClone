name: CloneRepo

on: 
  workflow_dispatch:
  push:

jobs:  
     job-public-repo-windows:
      runs-on: windows-latest
      steps:

        - name: rodalu/DemoClone PUBLIC repository
          uses: rodalu/DemoClone@main
          with:
              owner: 'rodalu'
              repository: 'DemoClone2'
      
        - name: Access cloned repository content
          run: |
            echo "ROOT"
            ls -la
            echo "CLONED REPO"
            cd DemoClone2
            ls -la
            shell: bash